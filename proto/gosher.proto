syntax = "proto3";

option go_package = "./schemas";

package schemas;

message Request {
    string msg = 1;
}

message Response {
    string msg = 1;
}

service Gosher {
    rpc SayHi (Request) returns (Response);
}